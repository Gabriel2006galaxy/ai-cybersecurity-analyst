# =========================================================
# AI-Based Cybersecurity Analyst Assistant
# Python dependencies for the full project (all phases)
# Install once with:  pip install -r requirements.txt
# =========================================================

# --- Core app / used starting Phase 0 ---
streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4

# --- Data visualisation (dashboard charts, used from Phase 1 onward) ---
plotly==5.24.1

# --- StandardScaler used in Phase 1 preprocessing; full anomaly
# detection / classification models arrive in Phase 2+ ---
scikit-learn==1.5.1

# --- Optional: Self-Organizing Map for anomaly clustering (Phase 2, optional) ---
minisom==2.3.3

# --- Generative AI summary via local Ollama (Phase 5+) ---
ollama==0.6.2

# --- Utilities ---
python-dateutil==2.9.0.post0
